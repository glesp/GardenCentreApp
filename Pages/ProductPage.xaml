<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GardenCentreApp.ViewModels"
             x:Class="GardenCentreApp.Pages.ProductPage">
    <ContentPage.BindingContext>
        <vm:ProductPageViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="10">

            <!-- Category Tabs -->
            <CollectionView ItemsSource="{Binding Categories}"
                            SelectionMode="Single"
                            SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                            HorizontalScrollBarVisibility="Never"
                            HeightRequest="50">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" Margin="5" BackgroundColor="LightGray" CornerRadius="20"
                               HasShadow="False" WidthRequest="100" HeightRequest="40">
                            <Label Text="{Binding}" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center" />
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Product List -->
            <CollectionView ItemsSource="{Binding Products}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10">
                            <Image Source="{Binding Image}" WidthRequest="100" HeightRequest="100" />
                            <Label Text="{Binding Name}" FontSize="16" />
                            <Label Text="{Binding Price, StringFormat='€{0:F2}'}" FontSize="14" />
                            <Button Text="Add to Basket"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ProductPageViewModel}}, Path=AddToBasketCommand}"
                                    CommandParameter="{Binding}"
                                    WidthRequest="200"
                                    HeightRequest="50"
                                    Margin="10" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
